export const workshopAdviceFormat={type:'json_schema',name:'workshop_advice',strict:true,schema:{type:'object',additionalProperties:false,required:['text','steps'],properties:{text:{type:'string'},steps:{type:'array',items:{type:'object',additionalProperties:false,required:['step','guidance'],properties:{step:{type:'integer'},guidance:{type:'string'}}}}}}};
export const workshopAdviceInstructions=` Return JSON with text (a direct answer, at most 150 words) and steps (up to 3 affected step numbers, 1 through 9, each with guidance at most 100 words). Tailor relevant current AND future steps to the user's concrete needs and already saved requests. A question alone needs no step updates. Step guidance must be practical, self-contained and consistent with the guide's exact dimensions, materials, joints and hardware. Merge earlier relevant adaptations for that step so they are not lost. Do not put unsupported structural changes into step guidance. For requests that need a different structural design or unsupported dimensions, explain the limitation and leave steps empty. For supported dimension changes tell the user to review and apply the design change; never claim dimensions changed in this response.`;
export function parseWorkshopAdvice(raw){const parsed=JSON.parse(raw);if(typeof parsed.text!=='string'||!Array.isArray(parsed.steps))throw Error('Invalid workshop response');return {text:parsed.text.slice(0,3000),stepUpdates:parsed.steps.filter(s=>Number.isInteger(s.step)&&s.step>=1&&s.step<=9&&typeof s.guidance==='string'&&s.guidance.trim()).slice(0,3).map(s=>({step:s.step,text:s.guidance.trim().slice(0,2000)}))};}
export function mergeStepAdvice(record,result,question,now=new Date().toISOString()){
 const next={...record.stepAdvice};for(const s of result.stepUpdates||[])next[s.step-1]={text:s.text,question,designKey:record.pack.build.key,createdAt:now};return next;
}
export function savedWorkshopContext(record){return Object.entries(record?.stepAdvice||{}).filter(([i,s])=>s.designKey===record?.pack?.build?.key).slice(0,9).map(([i,s])=>({step:Number(i)+1,request:String(s.question||'').slice(0,300),savedGuidance:String(s.text||'').slice(0,2000)}));}
