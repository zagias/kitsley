'use client';
import {useState} from 'react';
import ProjectArt from './project-art';
export default function ProjectHome({onStart,onUrgent,conversations=[],onResume}){
 const [text,setText]=useState('');const seen=new Set();
 const recent=[...conversations].filter(r=>!r.archived&&r.request?.trim()).sort((a,b)=>(Date.parse(b.createdAt)||0)-(Date.parse(a.createdAt)||0)).filter(r=>{const key=r.request.trim().toLowerCase();if(seen.has(key))return false;seen.add(key);return true;}).slice(0,3);
 return <section className="project-home"><div className="home-hero"><div className="home-main"><p className="page-kicker">YOUR DIY PROJECT, MADE CLEARER</p><h1>What would you like<br/>to get done?</h1><p className="home-intro">Tell Kitsley what you want to fix or make. We’ll work out the next step together.</p><form className="project-composer home-composer" onSubmit={e=>{e.preventDefault();if(text.trim())onStart(text.trim());}}><label className="sr-only" htmlFor="new-project">What would you like to fix or make?</label><textarea id="new-project" value={text} onChange={e=>setText(e.target.value)} maxLength={2000} placeholder="I want to renovate my bathroom…" rows="3" onKeyDown={e=>{if(e.key==='Enter'&&!e.shiftKey&&!e.nativeEvent.isComposing){e.preventDefault();e.currentTarget.form.requestSubmit();}}}/><div><span>Start free · no card needed</span><button className="primary" disabled={!text.trim()}>Start my project <span aria-hidden="true">→</span></button></div></form><p className="home-promise">A few simple questions. Useful advice. A plan when you need one.</p><a className="bc-home-link" href="/guides/bookcase">Try the illustrated bookcase project <span aria-hidden="true">→</span></a></div><div className="home-art"><ProjectArt/><div className="home-art-caption"><span>A little know-how.<br/><strong>A lot you can do.</strong></span><span className="small">Made for your<br/>next project.</span></div></div></div><section className="home-recent" aria-label="Recent searches"><div className="section-title-line"><h2>{recent.length?'Your recent searches':'Find a little inspiration'}</h2><a href="/library">Explore the library →</a></div>{recent.length?<div className="recent-project-links">{recent.map(r=><button key={r.id} onClick={()=>onResume(r.id)}><span>{r.request}</span><span aria-hidden="true">↗</span></button>)}</div>:<p>Browse free guides for ideas, repairs and everyday jobs around the home.</p>}</section><div className="home-urgent"><button onClick={onUrgent}>Something urgent? Get safety help →</button></div></section>;
}
